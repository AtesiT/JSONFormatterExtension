# 🎨 JSON Format Extension

A lightweight extension that automatically transforms raw JSON data into a **beautiful, readable format** directly in your browser.

> **Effect:** Turns messy raw text into structured, colorful code with syntax highlighting and a dark theme.

![Version](https://img.shields.io/badge/version-1.0-98c379?style=flat-square)
![Style](https://img.shields.io/badge/theme-Dark%20Mode-282c34?style=flat-square)

## 🚀 Installation

Since this extension is not yet in the Chrome Web Store, you need to install it manually:

1.  **Download** this repository (Code -> Download ZIP) and unzip it.
2.  Open Google Chrome and go to `chrome://extensions/`.
3.  Enable **Developer mode** in the top right corner.
4.  Click **Load unpacked**.
5.  Select the folder where you unzipped the files.

## ⚡ Usage

1.  Open any URL that returns **raw JSON** (API).
2.  The extension will **automatically** detect and format the content.
3.  Click the **Copy** button in the top right corner to grab the raw JSON data.
